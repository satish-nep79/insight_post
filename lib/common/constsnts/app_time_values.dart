class AppTimeValues {
  //Default Timers in MS
  static const defaultSplashTime = Duration(milliseconds: 2000);
  static const defaultDebounceTime = Duration(milliseconds: 1000);
  static const defaultSnackTime = Duration(milliseconds: 2000);
  static const defaultBottomNav = Duration(milliseconds: 500);

  //Api Timeouts
  static const int dioReceiveTimeout = 20000;
  static const int dioConnectionTimeout = 20000;
}
